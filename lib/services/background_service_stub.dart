// Stub used on Web (and as the fallback when dart.library.io is absent).
// Provides no-op implementations so main.dart compiles on all platforms.

void callbackDispatcher() {}

Future<void> registerBackgroundTasks() async {}
